-- =============================================================================
-- Tigim — migration 0014: новая роль `technologist`
-- =============================================================================
-- Зачем:
--   В реальных цехах основную операционку ведёт технолог: распределяет
--   работу, заводит сотрудников, заказывает фурнитуру, выдаёт зарплаты.
--   Но владелец не хочет показывать ему коммерцию — выручку, прибыль,
--   маржу. `manager` для этой задачи слишком широкий (видит финансы),
--   `staff` слишком узкий (read-only). Старый `master` (Мастер цеха)
--   был спрятан в UI ранее и теперь возвращается под именем «Технолог»,
--   но как отдельная enum-роль — чтобы старый master не получил
--   неявное расширение прав.
--
-- ВАЖНО: ALTER TYPE ADD VALUE нельзя использовать внутри транзакции
-- одновременно со ссылками на новое значение. Поэтому добавление
-- значения и RLS-политики разнесены: 0014 добавляет значение, 0015
-- использует его в policies. Применять В ПОРЯДКЕ: сначала 0014, потом 0015.
-- =============================================================================

alter type public.company_role add value if not exists 'technologist';

-- Done.
